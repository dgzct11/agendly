/**
 *  This is the page to either sync the user's calendar or download the ICS file
 * 
 */

export default function SyncPage() {
    return (
        <div>
            <h1>Sync Page</h1>
            <p>Sync your calendar or download ICS file</p>
        </div>
    );
}